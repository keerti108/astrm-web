"use client";

import InsoleVisual from "./InsoleVisual";

// WebGL canvas (InsoleCanvas.tsx) available for production.
// Using SVG/CSS animation for prototype reliability.
export default function HeroCanvas() {
  return <InsoleVisual />;
}
